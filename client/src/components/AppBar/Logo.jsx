import { Typography } from '@mui/material';

const Logo = () => (
  <Typography noWrap variant="h6" component="div" sx={{ display: { xs: 'none', sm: 'block' } }}>
    3D Model Hub
  </Typography>
);

export default Logo;
